

/**
 * @module generated (Vine)
 * @description This module includes and re-exports the Rust code that is
 * automatically generated by `tonic-build` from the `vine.proto` definition file.
 * It serves as the single source of truth for all gRPC message types, service
 * traits, and client/server structs.
 */

#![allow(non_snake_case, non_camel_case_types)]

// The `tonic::include_proto!` macro finds the file generated by `tonic-build`
// in the `OUT_DIR` and includes its contents here within a module named `vine_ipc`.
// The build script (`build.rs`) must be configured to compile `vine.proto` and
// name the package `vine_ipc`.
pub mod vine_ipc {
    tonic::include_proto!("vine_ipc");
}

// Re-exporting the most commonly used items for convenience. This allows other
// modules to `use crate::vine::generated::GenericRequest` instead of the longer
// `use crate::vine::generated::vine_ipc::GenericRequest`.
pub use vine_ipc::{
    cocoon_service_client::CocoonServiceClient,
    mountain_service_server::{MountainService, MountainServiceServer},
    CancelOperationRequest,
    Empty,
    GenericNotification,
    GenericRequest,
    GenericResponse,
    RpcDataPayload,
    RpcError,
};
