//! OTEL integration handle. Currently a placeholder.
/// OTEL telemetry service handle.
pub struct Struct;

/// Creates a new `Struct`.
impl Struct {
	pub fn new() -> Self { Self {} }
}

impl Default for Struct {
	fn default() -> Self { Self::new() }
}
